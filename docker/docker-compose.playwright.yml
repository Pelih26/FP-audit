services:
  playwright:
    build:
      context: ..
      dockerfile: docker/playwright/Dockerfile
    container_name: web_playwright
    working_dir: /app
    volumes:
      - ../:/app
    stdin_open: true
    tty: true
    network_mode: host
    command: tail -f /dev/null 